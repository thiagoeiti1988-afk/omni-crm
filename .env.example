# Copie para `.env` local. Nenhuma destas variáveis está ligada no protótipo ainda.

# Postgres / Supabase
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# MCP / webhooks (obrigatório antes de persistir writes)
MCP_API_KEY=

# RAG (Fase 2+)
EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=
